import subprocess

def record_call(output_path):
    """Use Asterisk Monitor() to record call to a WAV file"""
    # subprocess.run([
    #     "sudo", "asterisk", "-rx",
    #     f'MixMonitor(wav,{output_path},m)'
    # ])
    pass



